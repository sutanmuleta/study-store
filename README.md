
# Digital Study Resources Store

The goal of the project is to build a basic online store where users can browse digital study resources, add items to a cart, and check out.

## Project Structure

- `config/` – database connection file  
- `includes/` – reusable page parts (header and footer)  
- `public/` – main pages of the application  
- `assets/` – CSS and other static files  



